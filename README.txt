A very interesting program that verifies if columns were received in a medical file based on their name.

MED_Column_Repository are possible names the columns could be since they often change.

Summary is a summary of the file containing:
-Percent of Valid ICD codes (comparing to a database)
-Percent of Valid CPT codes (comparing to a database)
-Tells distinct values in columns
-Distinct paid year/month information